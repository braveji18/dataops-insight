# Iceberg 카탈로그 — StarRocks 와 "완전히 동일한" HMS + MinIO 를 가리킨다.
# 이 파일과 sql/00-starrocks-catalog.sql 은 항상 같은 값을 가리켜야 한다.
# 한쪽만 고치면 두 엔진이 다른 데이터를 보게 되고, 그 순간 모든 비교가 무효가 된다.
connector.name=iceberg
iceberg.catalog.type=hive_metastore
hive.metastore.uri=thrift://hive-metastore:9083

# Trino 470+ 는 레거시 hive s3 파일시스템이 제거되어 native S3 를 써야 한다.
fs.native-s3.enabled=true
s3.endpoint=http://minio:9000
s3.region=us-east-1
s3.path-style-access=true
s3.aws-access-key=minio
s3.aws-secret-key=minio123

iceberg.file-format=PARQUET
iceberg.register-table-procedure.enabled=true
