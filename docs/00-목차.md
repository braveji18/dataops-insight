# 결과물 목차 — Trino vs StarRocks 소스코드 비교

> 이 파일은 `plan/01-비교항목-정리.md` 의 비교 축을 **실제 산출 문서 단위로 분해한 목차**다.
> 계획(무엇을 비교할 것인가)은 `plan/01`, 진행 상태(무엇이 끝났는가)는 이 파일이 관리한다.
>
> 기준 버전: **Trino 483** / **StarRocks 4.0.14** (`scripts/testenv/versions.env` 와 `works/` 클론 태그가 일치)
> 최종 갱신: 2026-09-04

---

## 읽는 법

- **파일명 규칙**: `docs/<축>-<순번>-<제목>.md` (예: `A-2-01-조인-순서-결정.md`)
- **증거 파일**: `docs/evidence/<축>/<YYYYMMDD>-<probe>.md` — 본문의 주장을 실제 쿼리로 검증한 기록
- **문서 형식**: `plan/01` 5절 (비교 관점 → Trino 구현 → StarRocks 구현 → 설계 차이 → 영향 → 결론)

### 상태 표기

| 표기 | 뜻 |
|---|---|
| ✅ | 완료 (코드 인용 + 결론) |
| 🔬 | 완료 + **런타임 증거** 있음 |
| ⬜ | 미착수 |
| 🚧 | 현재 랩 구성으로 **검증 불가** — 코드 비교까지만 가능 |

### 검증 가능성의 한계 (전 축 공통)

현재 랩(`scripts/testenv/`)은 **Trino worker 1대 / StarRocks BE 1대, TPC-H SF 0.01** 이다. 따라서:

- **다중 노드에 반응하는 항목**은 검증 불가다. StarRocks의 broadcast 게이트(`beNum`), Trino의 `taskCountEstimator`, 셔플 전략, 리소스 격리가 모두 여기 해당한다
- **실행 시간 비교**는 전 축에서 불가하다. `perf` 프로파일(RAM 24GB + swap)이 필요하다 — `plan/02` 6-2절
- 위 두 경우 문서는 **코드 비교 + 설계 차이까지만** 쓰고, 성능 결론을 내지 않는다

---

## A. 핵심 (동일 기능의 구현 비교가 성립)

### A-1. SQL 파싱 & 분석 계층

| 문서 | 다루는 질문 | 상태 |
|---|---|---|
| `A-1-01-문법-정의와-방언-커버리지.md` | ANSI SQL vs MySQL 방언. 예약어 83 vs 155, 문장 91 vs 249, `sql_dialect='trino'` | 🔬 |
| `A-1-02-타입-체계.md` | DECIMAL 상한 38 vs 76, 시간대 타입 유무, 같은 Iceberg 스키마의 타입 표면 차이 | 🔬 |
| `A-1-03-함수-등록과-확장.md` | 애노테이션 vs `functions.py` 코드 생성. 내장 442 vs 637, UDF 확장 경로 | 🔬 |

**증거 파일**

| 파일 | 내용 |
|---|---|
| `evidence/A-1/20260903-parser-dialect-and-type-mapping.md` | `\|\|` 의미, 예약어, Iceberg 타입 매핑, Trino 방언 모드 판별 4종 |

### A-2. 옵티마이저 — **완료 (7/7)**

| 문서 | 다루는 질문 | 상태 |
|---|---|---|
| `A-2-01-조인-순서-결정.md` | 탐색 공간과 컷오프. 전수 열거 vs 규모별 알고리즘 전환 | 🔬 |
| `A-2-02-조인-분배-방식.md` | broadcast/shuffle 게이트. 바이트 기준 vs 행 수 기준 | 🔬 |
| `A-2-03-비용-모델.md` | 같은 탐색 공간에서 왜 다른 플랜을 고르는가 | 🔬 |
| `A-2-04-푸시다운-범위.md` | Predicate / Aggregation / Limit / TopN 푸시다운. 커넥터 협상 유무, 집계 푸시다운 임계값 2배 vs 100배 | 🔬 |
| `A-2-05-서브쿼리와-CTE.md` | 서브쿼리 언네스팅(15종 매트릭스), **CTE 재사용은 StarRocks 만 가능**, 윈도우 최적화 대상 모양의 어긋남 | 🔬 |
| `A-2-06-MV-쿼리-재작성.md` | **A-2 에서 유일한 명확한 기능 격차.** StarRocks 재작성 전용 18,000줄 vs Trino 재작성 규칙 0개. 신선도 정책도 정반대 | 🔬 |
| `A-2-07-통계-수집-주체.md` | 커넥터가 제공(Trino) vs 엔진이 직접 수집·관리(StarRocks). **통계는 공유되지 않는다** — 양쪽에서 각각 ANALYZE 필요 | 🔬 |

**증거 파일**

| 파일 | 내용 |
|---|---|
| `evidence/A-2/20260831-join-reorder-algorithm-threshold.md` | ⚠ **정정됨** — 관찰은 유효하나 원인 귀속이 틀렸다. 기록으로 보존 |
| `evidence/A-2/20260831-join-distribution-gate.md` | 게이트 임계값 이동으로 broadcast 경계 판별 |
| `evidence/A-2/20260903-cost-model-probes.md` | 판별 3종 + 2×2 교차 판별(위 정정의 근거) |
| `evidence/A-2/20260904-pushdown-scope-probes.md` | probe 7종. 6종은 동등, 조인 아래 집계만 결론이 갈림 |
| `evidence/A-2/20260904-subquery-cte-window-probes.md` | 상관 서브쿼리 15종 지원 매트릭스 + CTE 재사용 + 윈도우 4종 |
| `evidence/A-2/20260904-mv-query-rewrite-probes.md` | 양쪽에 같은 MV 를 만들고 재작성 여부·폭·신선도 정책 판별 (probe 6종) |
| `evidence/A-2/20260904-statistics-ownership-probes.md` | 통계 저장 위치·내용·공유 여부. StarRocks 가 술어 컬럼을 골라 자동 수집하는 장면 포착 |

### A-3. 실행 엔진 — **완료 (5/5)**

| 문서 | 다루는 질문 | 상태 |
|---|---|---|
| `A-3-01-데이터-표현과-벡터화.md` | `Page`/`Block`(Java) vs `Chunk`/`Column`(C++). 배치 기준 바이트 vs 행 수, 블록 11종 vs 컬럼 53종, **Trino 도 SIMD 를 쓴다(범위가 좁을 뿐)** | 🔬 |
| `A-3-02-실행-모델과-스케줄링.md` | ⚠ **`plan/01` 전제 정정** — 483 기본은 드라이버당 스레드. 양자 1초 vs 100ms, 다단계 큐 5단 vs 8단 | ✅ |
| `A-3-03-연산자-알고리즘.md` | 해시 조회표 2종+코드생성 vs 6종, **spill 가능 연산자 4계열 vs 12계열**, 정렬 구현 | ✅ |
| `A-3-04-런타임-필터.md` | 둘 다 기본 켜짐. 임계값 축(상한만 vs 상한+하한), TopN·집계 필터는 SR 만, 관측 경로 차이 | 🔬 |
| `A-3-05-표현식-평가와-딕셔너리.md` | 바이트코드 생성(필수) vs 벡터화 함수+JIT(선택). 딕셔너리 개입 층이 실행 중 vs 계획 시점 | 🔬 |

**증거 파일**

| 파일 | 내용 |
|---|---|
| `evidence/A-3/20260904-batch-unit-probes.md` | 배치 단위 노브 비교 + `chunk_size` 를 바꾸면 할당 메모리가 실제로 변한다 (probe 4종) |
| `evidence/A-3/20260904-runtime-filter-probes.md` | 양쪽 다 필터 생성. SR 은 `EXPLAIN VERBOSE` 에서만 보이고 조인 분배에 따라 `remote` 가 바뀐다 (probe 4종) |

### A-4. 데이터 교환 / 셔플 — **완료 (2/2, 코드 비교까지)**

| 문서 | 다루는 질문 | 상태 |
|---|---|---|
| `A-4-01-셔플-전송-계층.md` | **HTTP pull vs BRPC push.** 배압이 프로토콜에 내장 vs 응답 보류로 구현 | 🚧 |
| `A-4-02-셔플-파티셔닝과-스풀.md` | 파티셔닝 5+커넥터 vs 6종 고정. **셔플 스풀은 Trino FTE 만** | 🚧 |

> 두 문서 모두 **실측이 없다.** 1노드 랩에서는 원격 셔플 경로가 생기지 않는다. 코드 비교와 설계 차이까지만 썼다.

### A-5. 커넥터 / 페더레이션 — **완료 (4/4)**

| 문서 | 다루는 질문 | 상태 |
|---|---|---|
| `A-5-01-커넥터-인터페이스와-추가-난이도.md` | `trino-spi` 만 구현 vs FE/BE 양쪽 수정 — **확장성 관점의 핵심 차이** | ✅ |
| `A-5-02-커넥터-푸시다운.md` | 협상 메서드 11종 vs 없음. **함수 술어 프루닝은 어느 쪽도 상대의 상위집합이 아니다** | 🔬 |
| `A-5-03-파일-리더.md` | Java 리더 vs C++ 리더. 정상 경로는 동등, **갈리는 것은 결함의 폭발 반경** | 🔬 |
| `A-5-04-쓰기와-테이블-포맷-트랜잭션.md` | **StarRocks 는 Iceberg 에 추가만 가능하다** — UPDATE/DELETE/MERGE·유지보수 전무 | 🔬 |

**증거 파일**

| 파일 | 내용 |
|---|---|
| `evidence/A-5/20260820-starrocks-4.0.1-parquet-date-zonemap-crash.md` | C++ 리더 결함이 BE 프로세스를 통째로 죽인다. **arm64 한정 여부는 미확인** — 재개 절차는 `plan/02` 6-1절 |
| `evidence/A-5/20260904-connector-pruning-probes.md` | 파티션 프루닝 6종. 맨 컬럼은 동등, 함수 술어에서 서로 반대 방향으로 갈림 |
| `evidence/A-5/20260904-file-reader-compat-probes.md` | 복합 타입·코덱·삭제 파일 왕복 5종. 정상 경로 전부 일치 |
| `evidence/A-5/20260904-iceberg-write-capability-probes.md` | 같은 Iceberg 테이블에 DML/DDL 11종. 쓰기 격차 확정 |

---

## B. 운영·성능 영향

**완료 (6/6)**

| 문서 | 다루는 질문 | 상태 |
|---|---|---|
| `B-1-01-메모리-관리와-spill.md` | 30% 를 비우는가 90% 를 쓰는가. **StarRocks 는 적재·compaction·복제도 함께 회계한다** | 🔬 |
| `B-2-01-리소스-격리.md` | ⚠ **`plan/01` 전제 정정 — cgroup 이 아니다.** 입장 통제 vs 실행 통제 | 🔬 |
| `B-3-01-캐시-계층.md` | ⚠ **`plan/01` 전제 정정 — Trino 에도 파일 캐시가 있다.** 갈리는 것은 기본값 | 🔬 |
| `B-4-01-고가용성과-장애-복구.md` | 층마다 강약이 반대다. 노드 사망 감지 **62초 vs 즉시** | 🔬 |
| `B-5-01-보안과-권한-모델.md` | ⚠ **전제 보완 — StarRocks 에도 Ranger 가 있다.** 검사 지점 84 vs 31, OPA 는 Trino 만 | 🔬 |
| `B-6-01-관측성.md` | 쿼리 밖(계보)은 Trino, 쿼리 안(리더 내부 카운터)은 StarRocks | ✅ |

**증거 파일**

| 파일 | 내용 |
|---|---|
| `evidence/B/20260904-memory-limit-probes.md` | 한도 스윕. 유효 바닥 16MB vs 18MB, spill 은 판별 불가 |
| `evidence/B/20260904-resource-isolation-probes.md` | cgroup 부재 확인 + 실행 중 강제 여부(Trino 1kB 한도 통과 vs SR 즉시 중단) |
| `evidence/B/20260904-cache-layer-probes.md` | Trino 파일 캐시 존재 확인 + SR 캐시 적중 카운터 + Query Cache 는 OLAP 전용 |
| `evidence/B/20260904-node-failure-probes.md` | 실제 노드 정지. Trino 62.25초 재시도, SR 즉시 실패·19초 복구 |

---

## C. 한쪽에만 존재 (비교가 아니라 "부재와 대체 수단")

### C-1. StarRocks 전용 — 스토리지 계층

**완료 (4/4)**

| 문서 | 다루는 질문 | 상태 |
|---|---|---|
| `C-1-01-테이블-모델.md` | **같은 입력에 네 가지 결과.** `UPDATE`·부분 컬럼 갱신은 `PRIMARY KEY` 만 | 🔬 |
| `C-1-02-스토리지-포맷과-인덱스.md` | Tablet/Rowset/Segment + 파티션·버킷 2축. **GIN·VECTOR 는 실험 기능** | 🔬 |
| `C-1-03-compaction과-shared-data.md` | 자동 3종(cumulative/base/update) vs 사람이 부르는 `EXECUTE optimize` | 🚧 |
| `C-1-04-적재와-트랜잭션.md` | Routine Load 로 **DB 가 Kafka 구독을 소유**. `COMMITTED` ≠ `VISIBLE` | 🔬 |

**증거 파일**

| 파일 | 내용 |
|---|---|
| `evidence/C-1/20260904-table-model-probes.md` | 네 모델에 같은 입력. UPDATE 지원과 부분 컬럼 갱신까지 판별 |

### C-2. Trino 전용 — 플러그인 생태계

**완료 (2/2, 코드 비교까지)**

| 문서 | 다루는 질문 | 상태 |
|---|---|---|
| `C-2-01-Plugin-SPI.md` | **확장점 16 대 3.** `trino-spi` 558파일/73,858줄, `@Unstable` 계약 | ✅ |
| `C-2-02-커넥터-다양성과-게이트웨이.md` | 커넥터 27(+JDBC 15) 대 10. **관계형 DB 는 15:1** | ✅ |

---

## D. 코드베이스 자체

**완료 (2/2, 코드 계측)**

| 문서 | 다루는 질문 | 상태 |
|---|---|---|
| `D-01-빌드와-테스트-체계.md` | Maven 하나 vs Maven+CMake+Python 코드생성. **네이티브 의존성 59개** | ✅ |
| `D-02-코드-규모와-기여-난이도.md` | 본체 117만 vs 139만 줄. **Trino 는 플러그인이 30%** | ✅ |

> `plan/01` 이 D 축에 넣은 **"커밋 활동, 릴리스 주기"는 답하지 못했다** — `works/` 가 얕은 클론이라 이력이 없다.

---

## 종합

| 문서 | 내용 | 상태 |
|---|---|---|
| `99-종합-결론.md` | 31편의 결론을 **워크로드별 선택 기준**으로 재정리. 명확한 격차 / 대등한 것 / 답하지 못한 것 / **`plan/01` 전제 정정 7건** | ✅ |

---

## 보고용 문서 (`docs/report/`)

`docs/` 본문은 소스코드 인용이 많아 비전문가가 읽기 어렵다. 경영·관리 보고용으로 **같은 내용을 쉬운 용어 + GitHub 링크**로 다시 쓴 문서를 따로 둔다.

| 문서 | 대응 본문 | 상태 |
|---|---|---|
| `report/01-조인-순서-결정-보고.md` | `A-2-01` | ✅ |
| `report/02-조인-분배-방식-보고.md` | `A-2-02` | ✅ |
| `report/03-비용-모델-보고.md` | `A-2-03` | ✅ |
| `report/04-푸시다운-범위-보고.md` | `A-2-04` | ✅ |
| `report/05-서브쿼리와-CTE-보고.md` | `A-2-05` | ✅ |
| `report/06-MV-쿼리-재작성-보고.md` | `A-2-06` | ✅ |
| `report/07-통계-수집-주체-보고.md` | `A-2-07` | ✅ |
| `report/08-데이터-표현과-벡터화-보고.md` | `A-3-01` | ✅ |

**작성 절차는 스킬로 고정돼 있다** — `.claude/skills/exec-report/`. 원본 보강 → 코드 인용 확정 → GitHub 영구 링크 → 단위 환산 → "읽는 법" 해설 → 발췌 대조 → 목차 갱신의 7단계이고, 뼈대는 `references/template.md`, 발췌 정확성 대조는 `scripts/verify-code-blocks.py` 가 담당한다.

**작성 규칙**
- 전문용어 대신 일상어를 쓰고, 불가피하면 한 줄 설명을 붙인다
- 모든 기능 설명에 **GitHub 영구 링크**를 단다 (`blob/<태그>/<경로>#L<라인>` — 태그는 `483` / `4.0.14`)
- **확인하지 못한 것을 반드시 별도 절로 명시한다.** 보고서의 신뢰는 여기서 나온다
- 코드 기준값을 그대로 옮기지 말고 **읽는 사람이 쓰는 단위로 환산**한다(조인 개수 → 테이블 개수 등)

---

## 진행 순서

**전 축 완료 — 본문 31편 + 증거 18편.**

| 축 | 진행 | 비고 |
|---|---|---|
| A-1 파싱·분석 | 3/3 🔬 | |
| A-2 옵티마이저 | 7/7 🔬 | |
| A-3 실행 엔진 | 5/5 | |
| A-4 데이터 교환 | 2/2 🚧 | **실측 없음** — 1노드에서는 원격 셔플 경로가 생기지 않는다 |
| A-5 커넥터 | 4/4 🔬 | |
| B 운영·성능 | 6/6 🔬 | |
| C-1 스토리지 | 4/4 🔬 | |
| C-2 플러그인 | 2/2 | |
| D 코드베이스 | 2/2 | 커밋 활동·릴리스 주기는 **얕은 클론이라 답하지 못함** |
| 종합 | 1/1 | `99-종합-결론.md` |

**남은 것은 문서가 아니라 랩의 한계다.** `99-종합-결론.md` 5절이 목록을 관리한다.

| 미해결 | 필요한 것 |
|---|---|
| 실행 시간 비교 (전 축) | `perf` 프로파일 — RAM 24GB + swap (`plan/02` 6-2절) |
| 다중 노드 항목 (A-4 전체, A-2-02 `beNum`, B-2-01 CPU 분배, B-4-01 노드 사망) | 다중 노드 랩 (`plan/02` 6-3절) |
| StarRocks 4.0.1 크래시의 arm64 한정 여부 | 디스크 15GB 여유 (`plan/02` 6-1절) |
| 연합 질의, Ranger/OPA, Trino 파일 캐시·리소스 그룹, Kafka 적재 | 랩에 카탈로그·서비스 추가 |

> **보고서 진행**: 작성된 본문 31편 중 대응 보고서가 있는 것이 8편이다. 나머지는 `exec-report` 스킬로 이어 쓴다.

> **`plan/01` 전제 정정 7건과 코드 독해가 실측에 뒤집힌 2건**은 `99-종합-결론.md` 6절에 모여 있다.

### 축을 넘어 재등장하는 항목

문서를 쓰다 보면 같은 사실이 여러 축에서 근거로 쓰인다. 중복 서술하지 말고 **한 곳에 쓰고 링크**한다.

| 사실 | 1차 서술 위치 | 재등장 |
|---|---|---|
| 통계 부재 시 두 엔진의 행동 차이 | A-2-01 / A-2-03 | A-2-07 5-3, A-5-02 |
| C++ BE 결함의 폭발 반경(프로세스 전체 사망) | A-5-03 | B-4-01 |
| FE가 BE를 자동 블랙리스트에 올리는 동작 | B-4-01 | A-5-03 |
| 엔진 경계에서 누가 푸시다운 가능 여부를 판단하는가(커넥터 vs 엔진 하드코딩) | A-2-04 | A-5-01, A-5-02, C-2-01 |
| Trino 는 중간 결과를 물질화하지 않는다(CTE 재사용 부재의 뿌리) | A-2-05 | A-2-06, B-3-01, B-4-01 |
| MV 는 커넥터가 소유한다 — 그래서 옵티마이저가 손댈 수 없다 | A-2-06 | A-5-01, A-5-04, C-2-01 |
| 통계는 두 엔진 사이에 공유되지 않는다(양쪽에서 각각 ANALYZE) | A-2-07 | A-5-02, B-6-01 |
| 바이트 기준(Trino) vs 행 수 기준(StarRocks) — 게이트에서도 배치 단위에서도 | A-2-02 / A-3-01 | A-4-01, B-1-01 |
| 커넥터 추가에 FE/BE 양쪽 수정이 필요한 비대칭 | A-5-01 | C-2-01, D-02 |
| FE 타입 매핑과 BE 리더 지원 범위의 불일치 | A-1-02 | A-5-03, A-5-01 |
| 함수/커넥터/타입 모두 "SPI 뒤인가 코어 안인가"로 갈린다 | A-1-03 | A-5-01, C-2-01 |
